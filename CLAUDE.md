# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository (`gitmen-lab/seo`) is currently empty — no source files, build configuration, or commit history exist yet. There is no codebase structure, command set, or architecture to document.

When code is added to this repository, update this file to reflect:
- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and how major pieces of the codebase fit together
- Any project-specific conventions that aren't obvious from the code itself
